
		{{#> Form h1="Регистрация"}}
			{{> Input placeholder="Почта" class="primary" type="text" name="email" }}
			{{> Input placeholder="Логин" class="primary" type="text" name="login" }}
			{{> Input placeholder="Имя" class="primary" type="text" name="first_name" }}
			{{> Input placeholder="Фамилия" class="primary" type="text" name="second_name" }}
			{{> Input placeholder="Телефон" class="primary" type="tel" name="phone" title="Введите номер телефона в формате +7 999 999 99 99" isPhone=true }}
			{{> Input error="поле не может быть пустым" placeholder="Пароль" label="" class="primary" type="password" name="password" }}
			{{> Button label="Зарегистрироваться" class="primary" page="chat"}}
			{{> Link label="Войти " type="link" page="login"}}
		{{/Form}}
		
